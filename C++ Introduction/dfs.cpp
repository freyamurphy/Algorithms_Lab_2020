#include <iostream>
#include <stack>

void solveTest()
{
  // n = number of vertices
  // m = number of edges
  // v = starting vertex
  int n, m, v = 0;
  std::cin >> n >> m >> v;
  
  
  for (int i = 0; i < m; i++)
  {
    int a, b = 0;
    std::cin >> a >> b;
  }
}

int main() {
    std::ios_base::sync_with_stdio(false);

    int t = 0; std::cin >> t;   // number of tests
    
    for (int i = 0; i < t; i++)
    {
      solveTest();
    }
}